"""Sample projects for the ``timescaledb`` Python package.

Each sub-package under ``samples/`` is a self-contained, runnable example that
demonstrates TimescaleDB + time-series patterns using the ``timescaledb``
package and a TimescaleDB instance running in Docker.

See ``samples/README.md`` for the full catalog.
"""
